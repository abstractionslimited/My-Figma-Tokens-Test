{
  "name": "my-figma-tokens-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx token-transformer tokens.json output.json && npx style-dictionary build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "husky": "^9.1.7",
    "token-transformer": "^0.0.33"
  }
}
